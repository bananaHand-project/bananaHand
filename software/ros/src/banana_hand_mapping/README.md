# banana_hand_mapping

Hand landmark to joint mapping package (skeleton).

Purpose
- Subscribe to hand landmarks.
- Convert landmarks to joint angles (finger curl, spread, etc.).
- Publish target joint commands for teleoperation.
